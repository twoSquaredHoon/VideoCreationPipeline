# 이번 주 보고 — 영상 제작 파이프라인
**작성일:** 2026-08-06

---

## 1. 이슈 리포트: 숏폼 1건 제작에 2시간 이상 소요

1분 분량 숏폼 1건을 완성하는 데 **2시간을 살짝 넘게** 소요됨.

**원인**
- 클립 간 캐릭터(아동) 일관성 문제 — 외형/성별이 클립마다 흔들려 재작업 필요
- 특정 장면(투약 관련) 프롬프트의 의료 안전성 이슈 — 수정 및 재생성 필요. 이 과정에서 원본 블로그 글을 직접 다시 읽고 의학적으로 문제가 없는지 판단하는 시간도 별도로 소요됨
- 중간 흐름을 자연스럽게 잇기 위한 클립이 애초 설계에 부족 — 대사 문장 직접 선택 → 프롬프트 생성 → 영상 생성을 클립 3개에 대해 수동으로 반복
- 훅(Hook)·릴리프(Relief) 클립 일부 제거 및 교체

이 수동 개입 과정(블로그 재검토 및 의학적 판단 → 대사 선택 → 프롬프트 작성 → 영상 생성 반복)이 소요 시간의 대부분을 차지함. 특히 중간 흐름을 자연스럽게 이어가려는 시도가 가장 오래 걸림.

---

## 2. 개선 방향 (엔지니어링 솔루션)

**근본 원인:** 클립 디시전 / 프롬프트 생성 단계에서 캐릭터 일관성과 자연스러운 흐름이 처음부터 보장되지 않음 → 사후 수동 보정에 의존.

**제안:** 파이프라인 설계 단계에서 프롬프트 생성 로직을 재설계하여, 클립 간 전환이 자연스럽게 이어지고 캐릭터 일관성이 기본값으로 보장되도록 개선. (사후 재작업이 아닌 사전 방지 방식으로 전환)
- 애초에 클립 디시전 단계에서 더 많은 클립이 생성되도록 하여 중간 흐름 부족 문제를 사전에 방지
- 순수 정보 전달 목적 영상에서는 불필요한 훅(Hook)과 마지막 CTA 클립을 제거할 예정

---

## 3. 오늘(8/6) 배포된 엔지니어링 업데이트 (경미)

**0. Editing & pipeline UX (우선 반영)** — 기존 코드를 리버스 엔지니어링하여 코드와 프롬프트를 분리. 이제 프롬프트는 코드에 하드코딩되지 않고 앱 안에서만 존재하며, 직접 편집 가능:
- Browse의 **Prompts / Clips 탭**에서 클립별 상세 프롬프트(detailed prompt) 편집 및 저장, 저장한 프롬프트로 영상 재생성 가능
- **Voiceover 탭**에서 보이스오버 재생성/생성, Finder에서 보기, `voiceover.wav` 손상 감지 시 경고 표시
- 영상 재생성 시 클립 생성 로직이 저장된 detailed prompt를 우선적으로 사용하도록 개선

**그 외 업데이트**
1. **Browse → 수동 그룹** — 날짜 칩 제거, 사용자가 직접 그룹 생성 (신규 프로젝트는 기본 Ungrouped). `output/projects/project_groups.json`에 저장.
2. **프로젝트 드래그 앤 드롭** — 사이드바 그룹으로 드래그 이동 가능(AppKit drag/drop). 우클릭 / ⋯ 메뉴에도 **Move to group** 추가.
3. **프로젝트 이름 변경** — 카드 메뉴 **⋯ → Rename…**. `project.json`의 타이틀만 변경(디스크 폴더명은 유지).

**그 외 커밋:** Browse/프로젝트 정렬 개선, 클립 prompt 검수용 텍스트 처리 개선 (Python/Swift 클립 프롬프트 경로).

---

## 4. 추가 시간 단축 가능 요소

1. 현재 외부 Claude 클라이언트를 활용해 프롬프트 리뷰/수정 작업을 하고 있음 — 이 정도 수준의 분업은 적정하다고 판단.
2. *(미정 — 추가 논의 필요)*

---

## 요약

- **핵심 지표:** 1분 숏폼 1건 = 2시간+ (목표 대비 병목: 클립 일관성 + 중간 흐름 클립 부족)
- **단기 대응:** 수동 보정 (대사 선택 → 프롬프트 → 영상 생성 3개 추가)
- **중장기 해결책:** 프롬프트 생성 단계 재설계로 사전 방지 (클립 수 확대 + 정보성 영상의 훅/CTA 제거)
- **병행:** 앱 UX 개선 완료 — 코드/프롬프트 분리로 앱 내 프롬프트 편집·재생성 가능, 그룹핑, 이름 변경, 보이스오버 재생성
